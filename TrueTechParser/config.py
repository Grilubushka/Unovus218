"""
TrueTechParser — Configuration
Настройки поиска вакансий. Редактируйте этот файл под свои нужды.
"""

# ─── Поисковые запросы (ключевые слова) ──────────────────────────────────────
# Оставьте пустым [] чтобы получить все вакансии без фильтра по ключевым словам
SEARCH_QUERIES = [
    "",  # пустая строка = все вакансии (топ по умолчанию)
    # Примеры:
    # "Python",
    # "маркетолог",
    # "менеджер по продажам",
    # "аналитик данных",
]

# ─── Регион ───────────────────────────────────────────────────────────────────
# hh.ru area IDs: 1=Москва, 2=Санкт-Петербург, 113=Россия (вся)
HH_AREA_ID = 113  # Вся Россия

# ─── Количество вакансий на один запрос ──────────────────────────────────────
HH_PER_PAGE = 100         # hh.ru: максимум 100
HH_MAX_PAGES = 5          # Максимум страниц для каждого запроса
SUPERJOB_PER_PAGE = 50    # SuperJob: максимум 50
SUPERJOB_MAX_PAGES = 3
RABOTA_PER_PAGE = 20      # Rabota.ru
RABOTA_MAX_PAGES = 3

# ─── Расписание (APScheduler cron) ───────────────────────────────────────────
# Формат: каждые 4 часа = hour='*/4'
SCHEDULE_HOUR = "*/4"
SCHEDULE_MINUTE = "0"

# ─── Пути ─────────────────────────────────────────────────────────────────────
OUTPUT_DIR = "output"
OUTPUT_FILE = "vacancies.json"  # Итоговый файл с дедупликацией

# ─── HTTP настройки ───────────────────────────────────────────────────────────
REQUEST_TIMEOUT = 15  # секунд
REQUEST_DELAY = 1.5   # задержка между запросами (секунд), чтобы не попасть в бан

HEADERS = {
    "User-Agent": (
        "Mozilla/5.0 (Windows NT 10.0; Win64; x64) "
        "AppleWebKit/537.36 (KHTML, like Gecko) "
        "Chrome/124.0.0.0 Safari/537.36"
    ),
    # hh.ru API требует этот заголовок — без него возвращает 403
    # Формат: AppName/Version (contact@your-email.com)
    "HH-User-Agent": "TrueTechParser/1.0 (parser@truetechparser.local)",
    "Accept-Language": "ru-RU,ru;q=0.9,en-US;q=0.8,en;q=0.7",
    "Accept": "application/json",
}

# ─── SuperJob API ключ (опционально) ─────────────────────────────────────────
# Получить на: https://api.superjob.ru/
# Если пустой — будет использоваться HTML-парсинг
SUPERJOB_API_KEY = ""
